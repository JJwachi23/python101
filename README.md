## How to update code in GitHub
- Added code
  ```
  git add . 
  ```
  git add . it's means all of file change

  ```
  git add index.py
  ```
  select to added one file

- Comment
    ```
    git commit -m "your comment"
    ```
    git commit -m "" it's means comment code when your chage into the line of code

- Updated 
    ```
    git push
    ```
    git push it's means push code into GitHub to updated code to release